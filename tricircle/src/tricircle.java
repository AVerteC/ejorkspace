
import static java.lang.Math.pow;
import static java.lang.Math.round;

public class tricircle {
    public int sidelength;
    public int radius;

    public int circle(double radius){
        double d = 2.0;
        int area = (Math.PI*Math.ceil(pow(radius,2)));
    }

    public int triangle(int sidelength){

    }
}
